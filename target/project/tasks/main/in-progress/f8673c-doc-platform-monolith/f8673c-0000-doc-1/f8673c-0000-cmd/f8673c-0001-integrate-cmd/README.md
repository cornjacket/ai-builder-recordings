# integrate-cmd

## Goal

Cross-component synthesis: README.md for the cmd directory summarising the platform binary and its two service endpoints

## Context

### Level 1 — f8673c-0000-doc-1

### Level 2 — f8673c-0000-cmd
Command entrypoint subtree — contains the `platform` binary which wires together the metrics and IAM internal services


Atomic mode — no subdirectories. `authz.go` needs a companion `.md` (missing), and `README.md` is a placeholder stub. `authz_test.go` is skipped.
<response>
  <outcome>DOC_ARCHITECT_ATOMIC_DONE</outcome>
  <handoff>Created authz.go.md (missing — covers Role and UserRoleAssignment models, Handler public API, RegisterRoutes with route table, key internals for store concurrency and hasPermission logic, and uuid dependency). Updated README.md from a placeholder stub to a proper directory index with Purpose/Tags header, overview, file index table linking to authz.go.md, and four invariant notes (no persistence, append-only assignments, nil-safe permissions, role-existence enforcement). authz_test.go was skipped per policy. The authz directory is the RBAC sub-package of internal/iam — it owns roles, user-role assignments, and permission checks entirely in memory.</handoff>
  <documents_written>true</documents_written>
</response>

OUTCOME: DONE
HANDOFF: Atomic mode executed on `internal/iam/authz`. Created `authz.go.md` (was missing) covering the `Role` and `UserRoleAssignment` domain types, the `Handler` public API with a route table for all five RBAC endpoints, key internal logic for the `store` (RWMutex usage, `hasPermission` traversal, nil-normalisation in `addRole`), and the `github.com/google/uuid` dependency. Updated `README.md` from a placeholder stub to a proper directory index with Purpose/Tags header, one-sentence overview, file index table, and four package-level invariants (no persistence, append-only assignments, nil-safe permissions, role-existence check on assignment). `authz_test.go` was skipped per policy. Next agent will handle the sibling `lifecycle/` atomic directory.
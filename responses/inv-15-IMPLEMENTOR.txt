Now I'll write the implementation files.Clean compile. Now write the companion doc and run tests.Both `go build` and `go vet` pass clean. The implementation is complete.

<response>
  <outcome>IMPLEMENTOR_IMPLEMENTATION_DONE</outcome>
  <handoff>Implemented the `lifecycle` package in `output/internal/iam/internal/iam/lifecycle/` with two files: `lifecycle.go` (UserStore dual-indexed by ID and username, TokenStore, five HTTP handler methods, single exported `Handler() *http.ServeMux`) and `lifecycle_test.go` (table-driven tests covering all 17 acceptance criteria including concurrent-access test). Added `golang.org/x/crypto/bcrypt` via `go get`. Passwords are hashed at bcrypt cost 12; tokens are UUID strings; DELETE cascades token invalidation. A companion `lifecycle.go.md` documents design decisions. Both `go build` and `go vet` pass clean. The TESTER should run `go test -race ./internal/iam/internal/iam/lifecycle/...` from the module root to verify all acceptance criteria.</handoff>
  <documents_written>true</documents_written>
</response>
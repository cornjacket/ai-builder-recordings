OUTCOME: ACCEPTANCE_SPEC_WRITER_DONE
HANDOFF: Wrote `acceptance-spec.md` and `acceptance-spec.json` to `/Users/david/Go/src/github.com/cornjacket/ai-builder/acceptance-spec/sandbox/regressions/user-service/output`. 4 endpoints extracted: `POST /users`, `GET /users/{id}`, `PUT /users/{id}`, `DELETE /users/{id}`. Note: the spec only explicitly names status code 404 for GET, PUT, and DELETE; POST /users has no explicit status code in the spec, so `status_codes` for that endpoint is an empty array. Downstream ARCHITECT and coverage checker should be aware that success codes (e.g. 201 for POST, 200 for others) are not stated in the spec and were not inferred.
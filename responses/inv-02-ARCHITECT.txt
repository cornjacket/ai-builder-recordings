The acceptance spec is a stub (no endpoints extracted). The full API contract is in my prompt's `## Goal` section — I'll use that as the authoritative source.

Decomposing the service into three atomic components:

1. **store** (`internal/userservice/store`) — thread-safe in-memory map of user records; CRUD ops returning typed `User` structs.
2. **handlers** (`internal/userservice/handlers`) — HTTP handlers for the 4 CRUD endpoints, wired to the store interface.
3. **integrate-user-service** (`.`) — `main.go` wiring store + handlers into an `http.ServeMux`, listening on `:8080`.

The `User` struct needs fields. The Goal specifies a "JSON body" without enumerating fields. I'll use the standard minimal set: `id`, `name`, `email` — enough to make a meaningful user service without over-engineering.

Composite source dir for non-integrate components: `internal/userservice/` (common parent of `internal/userservice/store` and `internal/userservice/handlers`). I'll write `README.md` and `theory-of-operation.md` there.<response>
  <outcome>ARCHITECT_DECOMPOSITION_READY</outcome>
  <handoff>The user-service is decomposed into three atomic components. `store` owns all in-memory state behind a thread-safe CRUD interface. `handlers` maps the four HTTP endpoints to store calls and owns JSON encoding/decoding. `integrate-user-service` wires them into a single `main.go` listening on :8080. The User record has fields id (string, server-generated UUID), name (string), and email (string). Documentation has been written to internal/userservice/.</handoff>
  <components>
    <component>
      <name>store</name>
      <complexity>atomic</complexity>
      <source_dir>internal/userservice/store</source_dir>
      <description>Thread-safe in-memory store for User records. User struct: {"id":string,"name":string,"email":string}. Implements four operations: Create(user User) User (assigns a new UUID to id, stores and returns the record); Get(id string) (User, bool); Update(id string, user User) (User, bool) (replaces the record, preserves id, returns updated record and false if not found); Delete(id string) bool (returns false if not found). Uses sync.RWMutex for concurrent access. No persistence.</description>
    </component>
    <component>
      <name>handlers</name>
      <complexity>atomic</complexity>
      <source_dir>internal/userservice/handlers</source_dir>
      <description>HTTP CRUD handlers wired to the store interface. Full API contract:
POST /users {"name":string,"email":string} → 201 {"id":string,"name":string,"email":string} or 400 on bad JSON;
GET /users/{id} → 200 {"id":string,"name":string,"email":string} or 404 {"error":"not found"};
PUT /users/{id} {"name":string,"email":string} → 200 {"id":string,"name":string,"email":string} or 404 {"error":"not found"} or 400 on bad JSON;
DELETE /users/{id} → 204 (no body) or 404 {"error":"not found"}.
All responses use Content-Type: application/json. Routing via http.ServeMux with pattern /users and /users/{id}. Accepts a Store interface (Create/Get/Update/Delete methods as defined in the store component).</description>
    </component>
    <component>
      <name>integrate-user-service</name>
      <complexity>atomic</complexity>
      <source_dir>.</source_dir>
      <description>Wire all components into a cohesive unit and verify this level's acceptance criteria. Create main.go at the output root: initialise store.New(), pass to handlers.NewMux(store), call http.ListenAndServe(":8080", mux). End-to-end acceptance criteria: POST /users creates a user and returns 201 with generated id; GET /users/{id} returns 200 with the created user; GET /users/{unknown-id} returns 404; PUT /users/{id} returns 200 with updated fields; DELETE /users/{id} returns 204; DELETE /users/{unknown-id} returns 404. Module path: github.com/cornjacket/ai-builder/acceptance-spec/sandbox/regressions/user-service.</description>
    </component>
  </components>
</response>
This is atomic mode — `lifecycle/` contains only source files. I need to create `lifecycle.go.md` and replace the placeholder `README.md`.
<response>
  <outcome>DOC_ARCHITECT_ATOMIC_DONE</outcome>
  <handoff>Created lifecycle.go.md (missing — covers User/Token/Store/Handler types, New() and RegisterRoutes() public API with route table, key internals for Store concurrency model, addUser duplicate detection, handleLogin username-enumeration avoidance, handleLogout token extraction, and the errDuplicate sentinel type, plus uuid and bcrypt dependencies). Updated README.md from a placeholder stub to a proper directory index with Purpose/Tags header, one-sentence overview, file index table linking to lifecycle.go.md, and five invariant notes (no persistence, username uniqueness, no token expiry, password hash never serialised, concurrency safety). lifecycle_test.go was skipped per policy. The lifecycle directory is the user-CRUD and session-token sub-package of internal/iam.</handoff>
  <documents_written>true</documents_written>
</response>

OUTCOME: DONE
HANDOFF: Created `lifecycle.go.md` (new file — documents User, Token, Store, Handler types; New() and RegisterRoutes() public API with full route table; key internals covering Store RWMutex concurrency, addUser duplicate detection, handleLogin enumeration-avoidance, handleLogout Bearer extraction, and errDuplicate sentinel; uuid and bcrypt dependencies). Replaced the placeholder `README.md` with a proper directory index: Purpose/Tags header, one-sentence overview, file index table linking to lifecycle.go.md, and five cross-package invariants (no persistence, username uniqueness enforced, tokens have no TTL, PasswordHash never serialised, concurrent-safe via RWMutex). `lifecycle_test.go` was skipped per policy. Both files pass all linter rules. The next agent (integrate-iam) should document `iam.go` at the parent level and check whether the pre-existing `iam/README.md` is stale relative to the now-documented authz and lifecycle sub-packages.